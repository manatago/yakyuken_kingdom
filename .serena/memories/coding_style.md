GDScript uses 4-space indentation. Scene classes (scripts attached to .tscn) use PascalCase names (e.g., Table3D) while functions and exported variables stay snake_case. Favor signals over polling for interactions, and place helper scripts next to their corresponding scenes. Comments are sparse and only explain intent or tricky math; rely on descriptive node/folder names. Keep Godot resources/timelines updated in tandem when editing to avoid mismatched diffs.