Before wrapping a change, manually play through `Main.tscn` (or `TestDisplay.tscn` for HUD-only work) in Godot and describe the reproduction steps plus expected results in your README/PR notes, since automated regression coverage is limited. Run the PowerShell `run_tests_wsl.ps1` harness if script-covered tests are touched. When UI/scene behavior changes, capture screenshots/GIFs for the PR and mention the relevant `game_spec.md` section consulted. Keep Godot scene + script edits grouped in the same commit and verify no unintended binary/asset diffs slipped into `godot/assets/` or `godot/raw_images/`.